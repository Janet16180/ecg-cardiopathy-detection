# Versioned configuration

Use this directory for human-readable configuration shared by collaborators.
Keep credentials in local environment variables. Frozen executable experiment
manifests remain versioned by identity under `outputs/` and are linked from the
experiment queue; do not edit historical manifests in place.
